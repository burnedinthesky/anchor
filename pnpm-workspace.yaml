allowBuilds:
    # esbuild's install script places its platform binary.
    esbuild: true
    # Downloads the geckodriver binary for the real-Firefox check.
    geckodriver: true
    # Legacy transitive dep (web-ext tree); works without its build script.
    spawn-sync: false
